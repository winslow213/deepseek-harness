/** Host Loader entry for the team-account Sign out row browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the Sign out row. */
export function apply(): void {}
