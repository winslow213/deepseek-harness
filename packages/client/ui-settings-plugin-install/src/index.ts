/** Host Loader entry for the plugin-install tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin install tab. */
export function apply(): void {}
