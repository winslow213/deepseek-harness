{
  "name": "@quectel/dsh-team-account",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Account service for the dsh Team Shell: members, login sessions, agent-token issuance",
  "scripts": {
    "start": "node --import tsx/esm src/server.ts",
    "cli": "node --import tsx/esm src/cli.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.1",
    "@types/pg": "^8.11.10"
  }
}
